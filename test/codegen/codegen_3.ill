module Codegen3
  main :: String

  fn main ()
    list = Cons(1, Cons(2, Cons(3, Nil)))

    summed = foldr(plus, 0, list)
    show(list)
  end

  fn sum (Cons x xs)
    x + sum(xs)
  or sum (Nil)
    0
  end
end
