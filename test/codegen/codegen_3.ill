module Codegen3
  main :: String

  fn main ()
    # list = Cons(1, Cons(2, Nil))

    # mapped = fmap(fn (x) = x + 1 end, list)

    list = Cons(1, Nil)
    show(sum(list))
  end

  fn sum (Cons x xs)
    x + sum(xs)
  or sum (Nil)
    0
  end
end
